# =============================================================
# CodeMind AI 前端环境变量示例（占位符，禁止填写真实密钥）
#
# 使用方式：复制为 .env 后按需填写
#   cp .env.example .env
# =============================================================

# 后端 API 基础地址。
# 开发环境：留空或设为 /api（配合 vite.config.ts 的 /api 代理 → http://localhost:8080）
# 生产环境：设为网关/后端完整地址，如 https://api.example.com/api
# 注意：业务路径一律写成 /v1/...，此处只决定前缀。
VITE_API_BASE_URL=/api
